#!/bin/sh

. /lib/functions.sh

/etc/init.d/fika-iot-gateway enabled || return

[ "$ACTION" = "ifup" ] && {
    if [ ${INTERFACE} = 'wan' ]; then
        /etc/init.d/fika-iot-gateway restart
    fi
}
